// Vitest fails if there are zero tests. Remove this test if there are more than zero other tests.

import { expect, test } from "vitest";

test("Vitest works", () => {
  expect(true).toBeTruthy();
});
